{
  "name": "backend",
  "version": "1.0.0",
  "description": "BabycareMart Backend",
  "license": "ISC",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT",
    "server": "json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
